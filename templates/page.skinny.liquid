<div id="page" class="row">
  
  <div class="span12 details">
    <h1 class="p25">{{ page.title }}</h1>
    <div class="indent_max_reader ">
      {{ page.content }}
    </div>
  </div>
  <div class="span3">
    {% if page.title == 'sizing' %}
      <button onclick="goBack()">Go Back</button>
   {% endif %}
  </div>
  
</div>